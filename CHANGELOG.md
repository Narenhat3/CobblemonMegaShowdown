# 1.8.4+1.7.3-release
- Now showdown files only update once the game starts instead of everytime you open the game thus the config for disable Showdown loading has been disabled, now it only checks if the showdown is upto date instead of overwriting everytime
- 